CREATE TABLE XML_Documents (
    ID INT GENERATED ALWAYS AS IDENTITY,
    PRIMARY KEY (ID),
    XML_Content XML
   
);~

import from John_L_Hennessy.del OF DEL INSERT INTO XML_DOCUMENTS(XML_Content)~
import from David_A_Paterson.del OF DEL INSERT INTO XML_DOCUMENTS(XML_Content)~
